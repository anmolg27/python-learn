{
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); printf \"[%s] %s | %s\" \"$(echo \"$input\" | jq -r '.model.display_name')\" \"$(echo \"$input\" | jq -r '.workspace.current_dir | split(\"/\") | .[-1]')\" \"$(echo \"$input\" | jq -r '.output_style.name')\""
  }
}